<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Keep real files and folders reachable, but send application routes to React.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  RewriteRule ^ index.html [L]
</IfModule>
